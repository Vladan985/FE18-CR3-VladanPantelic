export interface ImenuItems{
    name: string;
    price: number;
    image: string;                      // to control the objects
    description : string;
    available : boolean;
    shortText : string;
}